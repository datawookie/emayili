library(hexSticker)

imgurl <- "docs/envelope-regular.svg"
sticker(imgurl,
        package="{imeyili}",
        p_size=8,
        s_x=1,
        s_y=.75,
        s_width=.6,
        spotlight = TRUE,
        filename="imeyili-hex.png")
